<% if resource.errors.present? %>
  <div>
    <h1><%= t('.header', count: resource.errors.count) %> found: </h1>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>