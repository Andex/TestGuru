en:
  activerecord:
    models:
      test: Test
      question: Question
      answer: Answer
      badge: Badge
    attributes:
      test:
        title: Title
        level: Level
        category: Category
        category_id: Category
        timer: Timer (in minutes)
      question:
        body: Question
      answer:
        body: Answer
        correct: Correct
      user:
        first_name: First name
        last_name: Last name
        email: Email
        password: Password
        password_confirmation: Password confirmation
      badge:
        name: Name
        picture: Picture
        rule_name: Rule
        parameter: Parameter
    gist:
      question: Question
      url: Gist URL
      user: User email
    errors:
      user:
        messages:
          not_numbers: must not start with a number or contain only numbers
          incorrect: incorrect
      answer:
        messages:
          maximum_amount: "The number of answers to the test exceeded, maximum: %{max}"
      messages:
        record_invalid: 'Validation failed: %{errors}'
        restrict_dependent_destroy:
          has_one: Cannot delete record because a dependent %{record} exists
          has_many: Cannot delete record because dependent %{record} exist